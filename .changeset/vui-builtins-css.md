---
"@fastkit/vui": minor
---

Add a `builtins.css` export (`@fastkit/vui/builtins.css`) that aggregates the built-in component stylesheets VUI depends on. Tooling can now pull the built-in styles through VUI itself instead of reaching into its individual dependencies.
