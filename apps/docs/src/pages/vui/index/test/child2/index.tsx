import { defineComponent } from 'vue';
import { VHero } from '@fastkit/vui';

export default defineComponent({
  render() {
    return (
      <div>
        <VHero>Test / child2 / index</VHero>
      </div>
    );
  },
});
