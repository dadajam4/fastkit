import { schema } from './schema';

export const en = schema.defineLocale.strict({
  translations: {
    lead: 'Toolkit to support rapid scratch development of applications.',
  },
});

export default en;
