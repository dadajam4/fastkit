export const VIRTUAL_MODULE_ID_PREFIX = 'virtual:ts-tiny-meta';

export const RESOLVED_VIRTUAL_MODULE_ID_PREFIX = `\0${VIRTUAL_MODULE_ID_PREFIX}`;

export const JSON_EXT_MATCH_RE = /\.json$/;

export const FILTER_RE = /\.?\$types(\.ts|\.json)?$/;
