/** Default rule name. */
export const RULE_DEFAULT_NAME = '__rule__';

/** Default name for function rules. */
export const FN_RULE_DEFAULT_NAME = '__fn-rule__';

/** Default rule message. */
export const RULE_DEFAULT_MESSAGE = 'The input value is incorrect.';
